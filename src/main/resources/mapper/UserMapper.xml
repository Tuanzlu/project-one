<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.UserMapper">

    <select id="getUserList" resultType="User">
        select * from test.user
    </select>

    <insert id="addUser" parameterType="map" useGeneratedKeys="true" keyColumn="userid">
        insert into test.user (username,pwd) values (#{username},#{pwd})
    </insert>

    <update id="updateUser" parameterType="map">
        update test.user  set username=#{username} where userid=#{userid}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from test.user where userid=#{userid}
    </delete>
</mapper>